package view;

import javax.swing.*;
import java.awt.*;

public class QuestionPanel extends JPanel {
    public QuestionPanel() {

        setSize(400, 400);
        setBackground(Color.GREEN);
        setVisible(true);
    }
}
