package view;

import javax.swing.*;
import java.awt.*;

public class HintPanel extends JPanel {

    public HintPanel() {

        setSize(400, 100);
        setBackground(Color.DARK_GRAY);
        setVisible(true);
    }
}
